import React from 'react';
import styled from 'styled-components';

const StyledTitle = styled.div`
  font-size: 72px;
  color: red;
`;

const Title = () => <StyledTitle>Title</StyledTitle>;

export default Title;
